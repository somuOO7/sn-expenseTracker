import { Label, SafeAreaContainer } from "@/components/ui";

const index = () => {
  return (
    <SafeAreaContainer style={{ flex: 1 }}>
      <Label>index</Label>
    </SafeAreaContainer>
  );
};

export default index;
